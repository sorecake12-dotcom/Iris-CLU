=== IRIS AI Voice Reference Directory ===

Place custom reference voice audio files here (.wav, .mp3, or .flac).

IRIS AI automatically scans this directory on startup and reloads automatically if a file is added, modified, or updated.

The first valid audio file found will be cached and used as the default cloned voice for all assistant speech responses.
